# JSOS TASK STATE

> 最后更新：2026-08-23（Asia/Shanghai）

## 当前目标

- 按已确认的《JSOS-Japanese Speaking OS V1.1》和 Figma V1.1 原型分阶段开发、测试并发布。
- 第一、二阶段开发及 Supabase 迁移已完成；正在发布网页，随后验收多用户、反馈和使用指南，再进入自定义计划与后台生成。
- 保持现有 Sites 外层公开、JSOS 应用内部登录保护，以及凌晨 1:00 训练日／顺延／补强逻辑。

## 已完成

- 现有生产版已使用 Supabase 东京区域，公开注册关闭，RLS 按用户隔离；40 个固定主题、400 条预热、总结导入、评分、补强、顺延和手动词句记录已上线。
- GitHub 私有仓库 `avrilray-np/jsos` 已配置，`main` 跟踪 `origin/main`；本轮改动尚未提交、推送或部署。
- V1.1 需求与原型已逐条确认；实施清单已写入 `docs/v1.1-implementation-plan.md`。
- 本地完成 V1.1 第一阶段：
  - 「昨日复习」改读前一个训练日的总结与手动词句，跨计划汇总并去重；不向更早日期回溯，空数据提示「昨日完成很好，无需复习」。
  - Word／Expression 总表改为累计读取当前与历史计划；首页当前计划统计仍只统计当前计划。
  - 总结导入先原样执行标准 JSON 解析；首次失败后才谨慎修复结构弯引号，不改合法字符串正文；失败保留输入并显示行列与稳定、可复制的错误编号。
  - 导入弹窗「稍后处理」改为「清除」；清除输入和错误但不关闭弹窗，空内容时不可点击。
  - 返回按钮根据浏览历史显示实际父页面；任务子页面可显示 `Day + 主题`，首页显示「返回首页」，直接进入使用默认父页面。
  - 每日任务详情文案改为「当日其他训练（打卡用不影响任务完成）」。
  - GPT‑Live 规范删除固定 N1／个人生活职场画像，保留根据现场表现调整语速、难度和提示量；正文引用统一使用 `「」`，JSON 结构仍使用 ASCII 引号；每日训练提示未修改。
- 用户完成两轮人工验收；根据反馈已继续调整：返回按钮增加间距并将「返回」设为墨绿色；「当日其他训练」保持 20px 黑色，只有「（打卡用，不影响任务完成）」使用 11px 灰色。
- 已修复跨 Day 错误导入：前端与 `/api/summaries` 接口同时校验当前页面任务编号，Day 10 页面不能再把 Day 9 JSON 写回 Day 9。
- 「昨日复习」已修正为相对当前打开的任务日期取前一训练日，例如 Day 8 读取 Day 7；完成状态也按日期分别保存，不再让所有历史任务共用当前真实日期的数据。
- 昨日复习在电脑和手机统一为一列；单词复用单词总表行样式，句子复用句子合集卡片样式。单词总表与复习单词均把读音移到单词下方。
- 已将总结提交窗口改为只允许当前训练日：页面禁用历史任务导入，API 再校验任务日期；新增本地迁移 `0007_summary_import_window.sql`，用于数据库层在凌晨 1:00 后阻止前一天及更早总结写入。
- 本地 `npm test` 通过完整构建与 16 项自动测试；`npm run lint` 无错误；`git diff --check` 通过。
- 用户已通过第一阶段第二轮验收。
- 本地完成 V1.1 第二阶段：
  - 登录取消单一邮箱环境变量限制，改为只允许 Supabase 中存在且启用的内测账号；公开注册仍关闭。
  - 新增首次登录修改密码提示，可选择「暂不修改」；头像设置中保留永久修改密码入口，密码最少 12 位。
  - 新增 `role`、`access_enabled` 和密码提示状态；普通用户对画像仅有读取权限，不能把自己改为管理员。
  - 新增实名反馈：正文最多 500 字、最多 5 张图、原图单张最多 10 MB、浏览器压缩后存储单张最多 2 MB、每人每天最多 10 次。
  - 反馈图片存入私有 Storage；管理员页面显示邮箱、时间、图片和未读／已读状态，可标为已读或同时删除反馈与图片。
  - 管理员权限由数据库 RLS 与接口同时校验；管理员不能因此读取其他用户训练数据。
  - 首页新增「使用指南」「提交反馈」；管理员额外显示反馈管理与未读数量。头像设置同步增加这些入口。
  - 登录页说明内测账号需联系管理员；计划设置按钮统一为「创建计划」。
  - 新增第二阶段配置文档 `docs/v1.1-phase-2-setup.md` 和迁移 `0008_accounts_feedback_and_permissions.sql`。
- 本地完整构建与 19 项自动测试通过；`npm run lint` 无错误；`git diff --check` 通过。
- 用户在自己的终端成功执行 `db push`；Codex 复核远端迁移历史已完整显示 `0001`–`0008`，确认 `0007`、`0008` 均已应用。
- 远端迁移后重新执行 `npm test`：生产构建及 19 项自动测试全部通过；`npm run lint` 无错误。

## 正在处理

- 第一、二阶段代码仍在本地工作区，正在提交、推送并部署；远端数据库迁移已经完成。
- 已确认远端数据库迁移历史到 `0006`，待执行正好是 `0007`、`0008`；Supabase CLI 写入凭证已失效。此前 `--no-browser` 验证码流程已取消。
- 用户已附加并确认正确的 Chrome Supabase 项目标签页；直接接管该外部标签页连续两次超时，已停止重复尝试。
- 尝试由 Codex 后台启动 Supabase CLI 自动浏览器授权，但用户侧没有打开授权页；该等待进程已明确取消。
- 已从老任务核对上次成功采用的方式：由用户在自己可见的 Mac 终端进入项目目录并运行 `npm run supabase -- login --output-format text --agent no`，完成浏览器授权后再由 Codex继续迁移。
- 用户已在可见终端完成 Supabase 登录；Codex 通过 `projects list` 验证登录成功，并确认链接到东京区 `JSOS` 项目 `lwhbotkqolffmfgptgni`。
- Codex 环境中的 `db push` 与只读预演曾停在 `Initialising login role...`；用户终端已成功完成推送，远端最终核验为 `0001`–`0008`。
- 使用指南当前是临时文字版：入口位于首页日历下方「帮助与反馈」区以及右上角头像设置；用户尚未提供该入口和页面的最终原型。
- Google Cloud 已确认正确的赠金项目（试用赠金约 NT$9,713，有效期至 2026-11-21）并启用 Agent Platform API。
- 组织安全策略禁止 API Key；已决定不放宽策略，后续在同项目部署最小 Cloud Run 生成服务，用专用低权限服务账号和 ADC 调用 Gemini。

## 当前问题

- `0007`、`0008` 已应用远端 Supabase；网页尚未完成生产部署。
- 迁移前自动备份未完成：Supabase CLI 的导出功能需要本机 Docker Desktop；迁移均包含事务，可在失败时整体回滚。
- 此前 V1.1 2.4 的「同一任务不限重新导入次数」已被本轮确认覆盖：仅当前训练日内可多次导入，凌晨 1:00 后关闭。
- JSOS 主站在 Google Cloud 外，不能直接取得项目 ADC；后续需要 Cloud Run 生成服务以及 JSOS 到该服务的受控鉴权。
- 当前 `AiProvider` 仍只有未配置占位实现；主题生成、每日完整内容生成、状态机、重试、幂等和 Gemini 调用尚未开发。
- 账号创建、管理员角色设置和反馈 Storage 的远端实测尚未进行；需要分别用普通用户和管理员账号验证 RLS。
- 自定义计划数据库结构、主题生成、每日内容生成和归档计划生成流程尚未实现。
- 外部 V1.1 原需求文档的生成服务文字仍可能残留 OpenAI 表述，代码实施以已确认的 Agent Platform Gemini／Cloud Run 方案为准，后续文档复核时统一修正。
- `npx tsc --noEmit` 仍受仓库既有 Cloudflare 类型缺失和 `SetupRequiredProvider` 返回类型错误影响；本轮 Vite/Vinext 正式构建已通过。

## 下一步

1. 提交并推送已验证的第一、二阶段代码，保存并部署新的 Sites 生产版本。
2. 在 Supabase Auth 创建不同随机初始密码的内测账号，把指定管理员画像的 `role` 设为 `admin`；不配置 SMTP。
3. 用普通用户验收登录、首次密码提示、修改密码、反馈限制和私有图片；用管理员验收未读数量、已读、删除及无权读取他人训练数据。
4. 验收第二阶段页面样式。
5. 进入第三阶段：实现 Step 1 主题生成／编辑／60 秒中断恢复、启用日期，以及 Step 2 后台逐日生成／三次重试／幂等恢复。
6. 创建 Cloud Run Gemini 生成服务、专用服务账号和预算提醒，完成结构化输出与实际费用测试。
