# JSOS TASK STATE

> 最后更新：2026-08-06（Asia/Shanghai）

## 当前目标

- 稳定运行“JSOS 管理训练闭环、GPT‑Live 负责实时对话”的单用户日语训练系统。
- 当前重点是发布并验收日历与总结导入、40 主题预热、移动端 App 跳转、熄屏恢复和新版补强判定。

## 已完成

- JSOS 已部署上线，使用 Supabase 东京区域；唯一账号和公开注册关闭方案已完成。
- 40 个主题及场景课程已装填，试运行计划已经启动。
- 已实现并验证凌晨 1 点（北京时间）的漏做自动顺延。
- 已新增补强调度迁移 `0005_reinforcement_scheduling.sql`：低分总结默认间隔两个完整训练日插入补强；手动补跑曾返回 `reinforcementCount: 1`、`reinforcementFailureCount: 0`。
- 已支持总结 JSON 的中文智能引号兼容、句子字段别名、历史已完成任务详情、真实评分／单词／句子展示。
- GPT‑Live 四份规范文本已升级到当前本地版本：项目指令 v3.1、Live 背景 v3.1、固定评分标准 v1.0、总结 JSON 规范 v1.0；内嵌 JSON 示例已通过标准解析检查。
- 已决定暂不增加训练时长记录功能；GPT‑Live 不得猜测时长，未知时使用 `durationMinutes: null`。
- 已只读核对线上 Day 4（补强任务“便利店与超市 2”）：Communication 3、Fluency 3、Pronunciation `null`、`coreGoalAchieved=false`、`needsReinforcement=true`；发音为空不是补强原因，再次补强由核心目标未独立完成触发。
- 本地已修复首页加载后固定回到计划起始月的问题，默认月份改为北京时间当前月。
- 本地已完成总结导入按钮状态：`验证并导入` → `正在保存…` → `已保存`，成功后隐藏“稍后处理”。
- 本地已把 Live 最终总结改为单一 `json` 代码块输出规范；JSOS 原有解析器已兼容代码块，可使用 ChatGPT“复制代码”。
- 本地已加强困难单词规则：明确想不起来、询问怎么说或用中文代替后获得的关键词必须进入 `newWords`；暂不增加候选单词勾选功能。
- 本地已新增通用“对话前预热”页面与“便利店与超市”10题，预热内容不会写入 Live 训练指令。
- 本地已为移动端增加先尝试 `chatgpt://`、失败回退 `https://chatgpt.com` 的打开逻辑；桌面端保持打开网页。
- `npm run lint`、生产构建、原有 6 项测试和新增 2 项预热数据测试均通过，`git diff --check` 通过。
- 本地已把“对话前预热”改为蓝绿色实心按钮，并为参考表达中的汉字逐段添加平假名振假名。
- 本地已移除手机端 ChatGPT 网页回退；移动端只调用 `chatgpt://`，桌面端仍打开 `https://chatgpt.com`。
- 本地已使用 `sessionStorage` 保存当前页面、任务编号和日历月份；iOS 重载后会在登录校验完成后恢复原位置，退出登录时清除状态。
- GPT‑Live 四份规范已放宽 `coreGoalAchieved`：主要流程和大多数核心目标独立完成即可；个别一至两个弱句进入句子复习，不触发整主题补强。
- 已在修改前只读确认 Day 4 没有下一级补强任务，并按用户授权更新线上唯一训练记录：`coreGoalAchieved=true`、`needsReinforcement=false`、`reasonCodes=[]`；复习重点保留。
- 第二轮修改后 `npm run lint` 无警告，生产构建、全部 8 项测试和 `git diff --check` 均通过。
- 第三轮本地样式调整已完成：“对话前预热”按钮改为 `#06C7B1`；逐字 ruby 振假名改为独立的平假名读音行，原日语句子不变。
- 日历补强红点已统一移到主题文字前，完成角标继续位于右上角；红点固定宽度，桌面和手机主题在剩余空间内保持单行并截断。
- 第三轮修改后 `npm run lint`、生产构建、全部 8 项测试和 `git diff --check` 均通过。
- 第四轮本地调整已完成：预热按钮保持 `#06C7B1` 并改为白字；恢复仅在汉字上方显示小号平假名，修复普通假名被误缩小导致的主体字号不统一。
- 补强红点缩小为 5px 并移到 `Day X` 前，主题恢复完整可用宽度；主题溢出改用省略处理，避免显示半个汉字。
- 第四轮修改后 `npm run lint`、生产构建、全部 8 项测试和 `git diff --check` 均通过。
- 本地已隐藏训练表现卡片中的时长模块，改为显示核心目标状态：达成显示绿色“目标达成！”，未达成显示橘黄色“等待补强…”，旧总结缺少字段时显示灰色“目标待确认”。
- 首页接口仅从训练总结提取 `coreGoalAchieved` 布尔值供页面使用，不会把完整原始总结返回浏览器。
- 核心目标状态修改后 `npm run lint`、生产构建、全部 8 项测试和 `git diff --check` 均通过。
- 本地已为现有 40 个课程主题各装填 10 条对话前预热句子，共 400 条；包含中文题目、自然日语参考表达和汉字平假名振假名。
- 已单独验证下一课“外食”包含推荐、配料、过敏、定制、催餐、错单、费用、分账和打包表达。
- 40 主题预热装填后 `npm run lint`、生产构建、全部 9 项测试和 `git diff --check` 均通过。

## 正在处理

- 正在提交、推送和部署全部已验证的本地改动。
- 继续观察 iPhone 熄屏恢复和从 ChatGPT App 返回 JSOS；页面位置持久化已实现。
- 等待确认后提交、推送和部署；当前代码仍只在本地。

## 当前问题

- GPT‑Live 过去出现来源检索失败、输出错误 JSON Schema 或中文 JSON 标点；v3.1 已把完整 JSON 硬契约内嵌到项目指令，但尚需真实会话验证。
- 过去角色扮演展开不足，可能用一个问题结束场景，或用“如果发生……怎么办”的教学式问题代替角色内事件。
- 过去总结可能只记录 1～2 个句子、没有单词，且把提示／跟读完成误判为掌握；新版要求把已证实困难的词和未独立完成的关键表达写入总结，尚需验证。
- 需确认 Day 2 的错误补强建议是否已用修正版总结覆盖，以及是否已经产生不应存在的补强任务。
- 线上 `/api/cron/daily` 的 `CRON_SECRET` 与本地值曾不一致，手动调用返回 401；Supabase 内部 pg_cron 不受影响。
- 当前工作树含未提交的四份规范文件修改及 `0005_reinforcement_scheduling.sql`，尚未完成版本库提交／推送核对。
- 单词目前完全依赖 Live 总结中的 `newWords`，用户在会话中不会表达的词可能被遗漏；需决定自动候选与用户勾选入库的产品规则。
- Day 4 总结实际返回 `newWords: []`，所以「値段」未入库；已修订三份 Live 规范，但需要下一次真实会话验证模型是否遵守。
- OpenAI 官方没有公开承诺可直接进入指定 ChatGPT Project／新聊天的移动深链；当前 `chatgpt://` 方案只能实机验证，已按用户确认移除网页回退。
- 真正的 iOS 页面重载仍会短暂出现“正在确认登录…”，但完成校验后应恢复此前页面；需要手机实测确认 Safari 的会话存储保留行为。
- 同一任务重新导入会覆盖评分、总结、补强结论和原始 JSON；句子会替换，但单词目前只追加／更新，不会移除前一次导入的旧单词。
- 凌晨1点前重新导入且补强尚未生成时，以最新 `needs_reinforcement` 为准；补强已经生成后，重新导入不会自动撤销既有补强任务。

## 下一步

1. 完成当前版本的提交、推送和生产部署。
2. 在线验收“外食”及其他主题的预热页面、目标状态和移动端页面恢复。
3. 用 iPhone 验证仅拉起 ChatGPT App、返回 JSOS、熄屏后恢复原页面。
4. 在 ChatGPT Project 替换项目指令并重新上传新版 Live 背景、评分标准与 JSON 规范，执行一次真实总结测试。
