# JSOS TASK STATE

> 最后更新：2026-08-11（Asia/Shanghai）

## 当前目标

- 稳定运行“JSOS 管理训练闭环、GPT‑Live 负责实时对话”的单用户日语训练系统。
- 正式训练前发布两个已完成的本地功能：凌晨 1 点训练日边界，以及任务详情内手动添加单词、句子。
- 线上数据库迁移与 Sites 发布尚未完成，不能先发布网页代码。

## 已完成

- JSOS 已部署上线，使用 Supabase 东京区域；唯一账号、公开注册关闭、40 个主题及 400 条对话前预热已完成。
- 已实现漏做自动顺延、低分补强调度、总结 JSON 导入兼容、历史任务详情、评分／单词／句子展示和目标达成状态。
- Sites 生产版本 4 已部署，当前已推送版本为 `6e35b6d`。
- 已分析 Day 5、Day 6、Day 7 完整 Live 对话，确认主要问题：教练依赖用户出题、场景推进不足、提前泄露目标表达并要求跟读、错误肯定、缺少即时纠错和复测、转写偏差被误用、总结证据夸大及单词／句子遗漏。
- 本地四份规范已完成新一轮修订：项目指令 v3.2、Live 背景 v3.2、固定评分标准 v1.0、总结 JSON 规范 v1.0。
- 项目指令与 Live 背景已结构性去重：项目指令独立承载启动、硬性行为、证据、结束流程和完整 JSON Schema；Live 背景只补充学习者画像、通用场景设计、对话节奏、救援和复盘方法，不再重复评分、补强和 JSON 规则。
- 新版规范明确：教练推动角色内事件；场景自然展开而不机械计数；关键错误执行纠正与复测；合理虚构训练信息允许但须内部一致；不得把任务清单当作完成事实；发音只按具体听觉证据判断，不假定退出 Live 后语音必然不可用；补强任务避免因单个弱句无限补强。
- 已移除交通主题偏向，场景变化统一使用适用于40个主题的信息变化、追加条件、对象不符、限制、误解、操作失败和临时需求等通用类型。
- Day 7 实测证明 Live 内口头说“请发总结”只得到口头确认，没有生成 JSON；已撤回该流程，恢复“退出 Voice 后在同一聊天用文字发送请发总结”。
- 四份规范已明确：训练场景／核心任务按实际沟通结果判定；目标表达只是教练内部参考，不要求逐字使用；自然同义表达同样有效；用户首次尝试前不得提前展示目标表达或要求跟读。
- JSON Schema 1.0 字段、层级和类型保持不变；没有新增场景字段。项目指令和 JSON 规范中的全部 JSON 示例已通过标准解析，`git diff --check` 已通过。
- 已核对计划切换逻辑：创建正式计划会立即归档当前试运行计划并创建全新 40 天活动计划；旧数据不删除，但当前首页、单词和句子只读取新活动计划，因此现有界面不会显示试运行数据。
- 已定位凌晨边界问题：前端目前按北京时间自然日期判断“今天”，00:00 就会提前切换到次日；数据库顺延任务仍在 01:00 执行。
- 已核对手动积累的数据限制：当前没有手动新增接口；`sentences.session_id` 不能为空，需通过数据库迁移支持不依赖训练总结的手动句子。
- 本地已实现 01:00 训练日：00:00～00:59 仍属于前一训练日；跨过 01:00 自动刷新，并在设备休眠跨日后回到页面时刷新数据且保留当前页面。
- 首页月历下已增加“每日25点更新任务，请在凌晨1点前上传数据。”；导入总结按钮下已增加“每日可多次练习，更新总结数据。”。
- 本地已实现任务详情“本次单词／本次句子”的用户添加入口：单词填写单词、平假名、中文释义；句子填写正确日语表达和中文意思；保存后进入本次记录和对应总表，来源显示“用户记录”。
- 已新增本地迁移 `0006_training_day_and_user_records.sql` 和用户记录接口；手动句子使用独立来源且不关联总结会话，因此重新导入 JSON 不会删除手动句子。
- 本地构建、代码检查和 12 项自动测试已全部通过，包含 00:59 与 01:00 边界测试；尚未应用线上数据库迁移、尚未部署生产站点。

## 正在处理

- 等待重新完成 Supabase CLI 登录，随后推送 `0006` 迁移。
- 数据库迁移成功后才能发布包含新查询字段与新接口的 Sites 版本。

## 当前问题

- 本地四份规范已经修改，但尚未上传到 ChatGPT Project，因此当前线上 GPT‑Live 不会自动使用本轮新规则。
- ChatGPT Voice 会后转写可能与实际语音不同；官方未明确说明后续文字回复能否重新访问原始音频，不能预设结论，Pronunciation 只按具体可靠证据判断。
- 单词仍完全依赖 Live 总结中的 `newWords`；候选单词勾选功能暂不实施。
- 同一任务重新导入会覆盖评分、总结、补强结论和句子；单词只追加／更新，不会移除旧词。补强已生成后，重新导入不会自动撤销既有补强任务。
- 仍需核对工作树中其他历史未提交内容和远端部署状态；本轮只修改了四份规范与本状态文件，没有部署。
- 当前归档计划没有查看入口；切换正式训练后试运行数据虽保留在数据库，但用户暂时无法在 JSOS 页面切回查看。
- 创建新计划会立即生成 40 个任务和日历排期；凌晨 1 点任务负责顺延、补强与当天状态调整，并不会每天重新生成全部课程内容。
- 当前总结重新导入会删除该训练会话的全部句子后重建；实现手动句子时必须区分 AI 总结与用户记录，重新导入只能替换 AI 记录，不能删除用户记录。
- Supabase CLI 当前提示访问令牌缺失；本轮尝试没有改变线上数据库。需要用户在本机终端重新运行登录流程。

## 下一步

1. 用户在项目目录重新执行 Supabase CLI 登录并告知完成。
2. 推送 `0006_training_day_and_user_records.sql`，确认线上表字段和 RPC 创建成功。
3. 重新构建并发布 Sites 生产版本，验证生产登录、首页读取与用户记录保存。
4. 用户创建正式训练计划；试运行计划归档且不参与新计划统计。
5. 继续用真实 Live 训练检查 Pronunciation 证据、代码块格式、`newWords`、`sentences` 和基于场景沟通结果的补强判断。
