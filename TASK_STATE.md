# JSOS TASK STATE

> 最后更新：2026-08-24（Asia/Shanghai）

## 当前目标

- 按已确认的《JSOS-Japanese Speaking OS V1.1》和 Figma V1.1 原型分阶段开发、测试并发布。
- 第一、二阶段已完成生产发布和验收；第三、四阶段的代码、数据库和 Gemini 生成服务已准备完成，下一步进入第五阶段上线验收。
- 保持 Sites 外层公开、JSOS 内部必须登录，以及凌晨 1:00 训练日／顺延／补强逻辑。

## 已完成

- 第一阶段已验收：昨日复习、跨计划词句累计、JSON 安全解析与兼容修复、当前训练日导入限制、返回按钮、详情文案及统一 GPT-Live 规范均已上线。
- 第二阶段已验收：Supabase 多账号登录、首次密码提示、修改密码、实名反馈、私有图片、管理员反馈管理，以及数据库 RLS 与接口双层权限控制均已上线。
- 当前生产版为 Sites 版本 7、GitHub `main` 提交 `36e55e1`，网址为 `https://jsos-japanese-speaking-os.avrilray.chatgpt.site`。
- 远端 Supabase 已应用迁移 `0001`–`0009`；本地与远端迁移清单一致。
- 第三阶段本地实现完成：计划草稿、1～90 天主题生成／编辑／60 秒恢复、启用日期、旧计划归档、逐日完整内容后台任务、三次自动重试、错误编号、手动重试和幂等恢复。
- Google Cloud 项目、赠金结算和 Vertex AI 权限已核对；专用运行账号仅授予 Vertex AI User，并采用 ADC，不使用 API Key。
- Gemini Gen2 云函数 `jsos-gemini-endpoint` 已在 `asia-northeast1` 部署并处于 ACTIVE；最小实例 0、最大实例 6、512 MB、120 秒超时，使用 `gemini-2.5-flash`。
- Gemini 服务支持主题、完整每日任务和谨慎 JSON 修复三类结构化生成；共享密钥、请求大小、超时、Schema 和脱敏用量日志已落实。
- 真实生成复测通过：3 天主题约 5.5 秒；完整每日任务约 16.9 秒，包含 3 个场景、5 条目标表达、10 条预热内容并通过 JSOS 结构校验。
- 按实测 token 与当前 Vertex AI 标准价估算，30 天完整计划约 US$0.28，不含失败重试；云函数按需启动成本预计很小。
- Sites 运行环境已保存 Gemini 服务地址、Gemini 密钥和新的 CRON 密钥；这些配置要等新版本部署后才生效。
- Google Cloud Scheduler 已创建一个每分钟后台任务并保持 PAUSED；JSOS 每次并行处理最多 6 天，目标为约 5 分钟完成 30 天内容。
- 已停用错误的旧服务密钥版本 1，版本 2 正常；停用后真实生成复测通过。本地临时密钥副本已删除。
- 当前完整构建、主站 25 项测试、生成服务 4 项测试、ESLint 和 `git diff --check` 均通过。

## 正在处理

- 第三、四阶段代码仍只在本地工作区，尚未提交、推送或保存为新的 Sites 版本。
- 远端 Supabase `0009` 和 Gemini 云函数已经就绪；Sites 生产版仍为版本 7，不包含新计划生成界面与接口。
- 后台 Scheduler 保持暂停，避免在新版 Sites 发布前请求尚不存在的生产接口。
- 使用指南仍是临时文字版；用户尚未提供最终页面原型。

## 当前问题

- 下一步推送 GitHub `main`、保存 Sites 验收版本并部署公开生产站，需要用户明确确认本轮发布。
- 发布后需要先手动执行一次后台任务并完成新用户端到端验收，再恢复每分钟自动执行。
- Google Cloud 预算提醒金额尚未确认；建议按低用量内测设置项目级月预算并启用 50%／80%／100% 邮件提醒。
- 两个早期独立 Cloud Run 服务的默认 `run.app` 地址仍返回平台级 404；当前已改用工作正常的 Gen2 Cloud Function 地址，它们均为最小实例 0，后续可确认后清理。
- 外部 V1.1 需求文档仍可能残留 OpenAI／Cloud Run 表述；实施以 Gemini Gen2 云函数方案为准，后续统一修正文档。
- `npx tsc --noEmit` 仍受仓库既有 Cloudflare 类型缺失影响；Vite/Vinext 正式构建已通过。

## 下一步

1. 取得用户授权后提交并推送第三、四阶段代码，打包并保存新的 Sites 验收版本。
2. 用户确认后部署到生产，手动运行一次后台任务，验收主题生成、日期选择、30 天后台内容、5 分钟提示、失败恢复和切换计划。
3. 验收正常后恢复 Scheduler 自动运行，并检查 Sites、Supabase 和 Gemini 日志。
4. 根据用户确认的金额创建项目级 Google Cloud 预算提醒；之后清理未使用的早期 Cloud Run 服务并统一更新需求文档。
