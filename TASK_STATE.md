# JSOS TASK STATE

> 最后更新：2026-08-23（Asia/Shanghai）

## 当前目标

- 按已确认的《JSOS-Japanese Speaking OS V1.1》和 Figma V1.1 原型分阶段开发、测试并发布。
- 第一阶段已验收；第二阶段「多用户基础功能」的功能、数据库迁移、生产发布和双角色验收均已完成，当前补充第二阶段的设置层级与管理员未读提醒样式。
- 保持 Sites 外层公开、JSOS 内部必须登录，以及凌晨 1:00 训练日／顺延／补强逻辑。

## 已完成

- 第一阶段已完成并验收：昨日复习、跨计划词句累计、JSON 安全解析与兼容修复、当前训练日导入限制、返回按钮、详情文案及统一 GPT-Live 规范均已上线。
- 第二阶段账号与反馈功能已上线：Supabase 多账号登录、首次密码提示、永久修改密码、实名反馈、私有图片、管理员未读／已读／删除、数据库 RLS 与接口双重权限控制。
- 远端 Supabase 迁移 `0001`–`0008` 已全部应用；公开注册关闭，普通用户训练数据继续按 `auth.uid() = user_id` 隔离，管理员仅可跨用户管理反馈。
- GitHub 私有仓库 `avrilray-np/jsos` 的 `main` 跟踪 `origin/main`；当前生产版为 Sites 版本 6、提交 `d468a08`，网址为 `https://jsos-japanese-speaking-os.avrilray.chatgpt.site`。
- 普通用户生产验收通过：登录、首次密码提示、指南、反馈、无管理员入口、带图反馈、拒绝直接访问管理员页、修改密码后重新登录均正常。
- 管理员生产验收通过：未读数量、反馈邮箱／时间／正文／私有图片、标为已读与删除均正常；测试反馈和图片已清理。
- 本地完成本轮设置体验调整：
  - 右上角头像改为只打开「账号设置」，其中新增与「使用指南」平级的「训练计划设置」入口。
  - 首页无计划状态的「开启训练计划」直接进入训练计划设置。
  - 新用户进入计划设置后直接显示计划类型、Day 1 日期和「创建计划」。
  - 已有计划用户默认只显示当前计划、归档数量、说明文字和「重新创建计划」；点击后才展开创建表单。
  - 管理员存在未读反馈时头像变为红色，不显示数字角标。
- 本轮 `npm test` 通过完整构建与 19 项自动测试；`npm run lint` 无错误；`git diff --check` 通过。

## 正在处理

- 本轮设置体验调整已在本地提交；尚未推送到 GitHub，也未保存为新的 Sites 版本或部署到生产。
- 使用指南当前仍是临时文字版；入口已存在，用户尚未提供最终页面原型。
- Google Cloud 已确认赠金项目（约 NT$9,713，有效期至 2026-11-21）并启用 Agent Platform API；组织策略禁止 API Key，后续采用同项目 Cloud Run、专用低权限服务账号与 ADC 调用 Gemini。

## 当前问题

- 本轮设置体验调整需要生成新的 Sites 版本供验收；正式覆盖生产环境前需要用户明确确认。
- `AiProvider` 仍只有未配置占位实现；主题生成、每日完整内容生成、状态机、重试、幂等和 Gemini 调用尚未开发。
- JSOS 主站在 Google Cloud 外，不能直接取得项目 ADC；后续需要 Cloud Run 生成服务以及 JSOS 到该服务的受控鉴权。
- 自定义计划数据库结构、主题生成、每日内容生成和归档计划生成流程尚未实现。
- 外部 V1.1 原需求文档可能仍残留 OpenAI 表述；代码实施以已确认的 Gemini／Cloud Run 方案为准，后续文档复核时统一修正。
- `npx tsc --noEmit` 仍受仓库既有 Cloudflare 类型缺失和 `SetupRequiredProvider` 返回类型错误影响；Vite/Vinext 正式构建已通过。

## 下一步

1. 用户明确授权后推送本轮设置体验调整，保存新的 Sites 版本；再次取得生产发布确认后部署并进行管理员／普通用户页面验收。
2. 用户确认临时使用指南是否可作为本期版本；第二阶段通过后添加其余 3 个内测账号。
3. 进入第三阶段：实现 Step 1 主题生成／编辑／60 秒中断恢复、启用日期，以及 Step 2 后台逐日生成／三次重试／幂等恢复。
4. 创建 Cloud Run Gemini 生成服务、专用服务账号和预算提醒，完成结构化输出与实际费用测试。
